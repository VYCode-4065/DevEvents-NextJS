import Event, { IEvent } from "./event.model";
import Booking, { IBooking } from "./booking.model";

// Export models and interfaces for application-wide use
export { Event, Booking };
export type { IEvent, IBooking };
